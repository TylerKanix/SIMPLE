# Generated by build.js. Regenerate with `node build.js` rather than committing
# it — a stale bundle in the tree is worse than no bundle at all.
mandate.html

# Generated by publish.js — the artifact copy of the bundle.
mandate.artifact.html
